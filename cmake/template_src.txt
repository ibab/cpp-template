
# Specify .cpp files
set(SRC hello.cpp)

# Specify the executable
add_executable({{project_name}} main.cpp ${SRC})
target_link_libraries({{project_name}} ${LIBRARIES})

# Build a library
add_library(_{{project_name}} ${SRC})
target_link_libraries(_{{project_name}} ${LIBRARIES})

# vim:ft=cmake


